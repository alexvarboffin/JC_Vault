

  Роль: Senior Android UI Engineer (Expert in Jetpack Compose & Material 3).

  Задача: Выполнить Pixel-Perfect доработку компонента [ВСТАВЬ_ИМЯ_КОМПОНЕНТА], используя в качестве эталона скриншот
  [ВСТАВЬ_ИМЯ_ФАЙЛА_СКРИНШОТА] из папки Documentation.

  Алгоритм действий:

   1. Визуальный реверс-инжиниринг:
       * Проанализируй скриншот: определи иерархию (Row/Column), отступы (padding/spacing), скругления углов
         (shape/radius).
       * Разбери типографику: определи размеры (sp), веса (Bold/Medium) и выравнивание текста.
       * Определи состояния: активна ли кнопка, есть ли ошибки, фокус на полях.

   2. Анализ текущего кода:
       * Найди и прочитай файл компонента в Android-проекте.
       * Сопоставь текущие параметры (размеры, цвета из темы) с тем, что видишь на дизайне.

   3. Имплементация (Execution):
       * Внеси изменения для достижения 100% визуального сходства.
       * Приоритет UI-компонентов: Если на дизайне модальное окно — используй ModalBottomSheet.
       * Используй только существующие токены проекта: FigmaBrandBlue, FigmaTitleColor, FigmaCardWhite,
         LdsOutlinedButton, MyTextField.

  СТРОГИЕ ПРАВИЛА (Base Prompt Constraints):

   * ТОЛЬКО ТОЧЕЧНЫЕ ПРАВКИ (Surgical Edits): Категорически запрещено перезаписывать файл целиком или использовать
     write_file для существующих файлов. Используй только replace.
   * СОХРАНЕНИЕ КОНТЕКСТА: Запрещено удалять закомментированный код и пустые строки. Они — часть рабочего окружения
     пользователя.
   * НИЧЕГО ЛИШНЕГО: Не добавляй новую бизнес-логику, не меняй API ViewModel и не придумывай элементы, которых нет на
     скриншоте.
   * ВАЛИДАЦИЯ: После правок обязательно запусти сборку модуля app через .\gradlew.bat :app:assembleDebug.

  Формат результата:
   1. Audit Report: Список визуальных несоответствий (Spacing, Typography, Components).
   2. Surgical Diff: Результат применения точечных правок.
   3. Build Status: Подтверждение успешной компиляции.

  ---

  Инструкция по применению:
  Вставь название компонента (например, FreeWifiSettingsContent) и имя файла (например, Screenshot_20260416_112546.png).
  Агент проанализирует картинку и точечно поправит код.