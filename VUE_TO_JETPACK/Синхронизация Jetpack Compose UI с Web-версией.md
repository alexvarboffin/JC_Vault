

  РОЛЬ: Senior Android UI Engineer (Jetpack Compose, Material 3).

  Задача: Синхронизировать UI и логику Android-компонента [ВСТАВЬ_ИМЯ_КОМПОНЕНТА_ИЛИ_ФУНКЦИИ] с его оригинальной
  реализацией в Web-проекте.

  Алгоритм действий:

   1. Исследование (Research):
       * Найди файл в Android-проекте (@@@@@@@), где определен целевой компонент. Проанализируй его текущую
         структуру, используемую ViewModel и Contract.
       * Найди соответствующий Vue-компонент в Web-проекте (@@@@\app\workspaces\frontend\src).
         Используй grep_search по ключевым словам для поиска .vue файлов, конфигов в modalConfigs.ts и сервисов в
         services.service.ts.

   2. Анализ Web-реализации:
       * Разбери структуру Layout (Row/Column), отступы (padding/gap), типографику (размеры, веса шрифтов) и цвета
         (переменные SCSS).
       * Изучи бизнес-логику фронтенда: условия доступности кнопок, генераторы данных (например, случайные пароли),
         форматирование ввода.

   3. Синхронизация (Execution):
       * Обнови Android-компонент так, чтобы он визуально и функционально на 100% соответствовал Web-версии.
       * ВАЖНО: Если это диалог, используй ModalBottomSheet (если не указано иное).
       * Используй существующие токены темы: FigmaBrandBlue, FigmaTitleColor, FigmaCardWhite и системные компоненты:
         LdsOutlinedButton, MyTextField, PasswordInputComponent.

  Технические ограничения и правила:

   * ТОЛЬКО ТОЧЕЧНЫЕ ПРАВКИ: Запрещено перезаписывать файлы целиком. Используй инструмент replace для хирургического
     изменения конкретных строк.
   * СОХРАННОСТЬ КОНТЕКСТА: Категорически запрещено удалять закомментированный пользователем код и пустые строки.
   * АРХИТЕКТУРА: Строго следуй паттерну MVI/MVVM проекта. Не меняй публичные API существующих ViewModel, если это не
     требуется для передачи новых данных.
   * РЕСУРСЫ: Если в Web используются иконки (например, dice), найди их в папке ресурсов или попроси пользователя
     предоставить Android Vector Drawable.

  Формат результата:
   1. Список выявленных расхождений между Web и Android.
   2. Результат выполнения точечных правок через replace.
   3. Подтверждение успешной сборки модуля app через Gradle.
