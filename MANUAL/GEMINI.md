robocopy "C:\Users\combo\AppData\Roaming\npm" "C:\Users\lazagne47pro\AppData\Roaming\npm" /E /COPYALL


npm install -g @google/gemini-cli@latest
npm install -g @google/gemini-cli@latest  
npm install -g @google/gemini-cli@preview

npm install -g @google/gemini-cli
gemini extensions install https://github.com/figma/figma-gemini-cli-extension


/derectory add @@@@\app\workspaces\frontend
gemini --resume 1 /directory add C:\SYNCED\ldsonline

```
gemini --include-directories "../server"
```


я добавил в проект заготовку :kmp:accesspoint давай переместим в нее из модуля :accesspoint классы, чтобы проект собирался!

Замена для KMP
import org.json НА import kotlinx.serialization.json

https://klibs.io/project/ComposeComponents/Parcelable это нужно для уже перенесенных kmp Parcelable и @ Parcelize

весь файл не перезаписываем!, только точечные целевые правки

com.russhwolf:multiplatform-settings это для хранения настроек в kmp, тоесть не используем Android
   sharedpreference (примеры применения библиотеки в коде уже есть)
   
   Prioritizing commonMain
   
 наша цель получить 100% рабочее kmp с полноценным функционалом, при этом сборка kmp android должна
   обладать всеми функциями и возможностями как и оригинал приложения до перехода на kmp
   
   
   
   export NO_BROWSER=true